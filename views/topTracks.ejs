<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Ninja | <%= title %></title>
</head>
<body>
    <h1>These are the list artist-top-tracks for artist of id=40008598</h1>
    <br>
    <% if (topList.length > 0) { %>
        <% topList.forEach(song => { %>
            <ul>
                <li><%= song %></li>
            </ul>
    <% }) %>
    <% } else { %>
        <p>There are no songs to display...</p>
    <% } %>
</body>
</html>